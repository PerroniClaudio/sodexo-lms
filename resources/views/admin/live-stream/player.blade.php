<x-layouts.admin>
    <section class="mx-auto max-w-5xl px-6 py-16">
        <div class="rounded-3xl border border-base-300 bg-base-200 p-8 shadow-sm">
            <span class="badge badge-neutral badge-outline mb-4">{{ __('Admin') }}</span>
            <h1 class="text-3xl font-semibold">{{ __('Player live admin') }}</h1>
            <p class="mt-3 text-base-content/70">
                {{ __('Questa area mostra il player della live gestito dagli amministratori.') }}
            </p>
        </div>
    </section>
</x-layouts.admin>
